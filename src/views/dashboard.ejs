<!DOCTYPE html>
<html>
<head>
  <title>Dashboard</title>
  <style>
    body { font-family: Arial; padding: 20px; }
    .card {
      border: 1px solid #ccc;
      padding: 16px;
      max-width: 400px;
      margin: auto;
      text-align: center;
      border-radius: 8px;
    }
    img {
      border-radius: 50%;
      margin-bottom: 16px;
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>Welcome, <%= user.name %>!</h2>
    <img src="<%= user.avatarUrl %>" alt="Profile Picture" width="100" height="100" />
    <p><strong>Email:</strong> <%= user.email %></p>
  </div>
</body>
</html>
